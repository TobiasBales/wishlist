<%= turbo_stream.replace dom_id(@list, :new_item) do %>
  <%= turbo_frame_tag dom_id(@list, :new_item) do %>
    <%= render(C::Items::New.new(list: @list, item: @item)) %>
  <% end %>
<% end %>

<%= turbo_stream.replace dom_id(@list) do %>
  <% turbo_frame_tag dom_id(@list), src: list_path(@list) %>
<% end %>
