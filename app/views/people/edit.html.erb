<% page_title(t("people.edit.page_title", person: @person.name)) %>
<%= render(B::Page.new(crumbs: [
  B::Crumb.new(text: t("people.index.title"), to: people_path),
  B::Crumb.new(text: t("people.edit.title", person: @person.name), to: edit_person_path(@person))
])) do %>
  <%= render "form", person: @person %>
<% end %>
